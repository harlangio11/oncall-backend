{
  "name": "oncall-backend",
  "version": "1.0.0",
  "description": "OnCall backend — Twilio webhook handler + APNs critical alert push",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@parse/node-apn": "^6.1.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
